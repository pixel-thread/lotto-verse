// Global TTL for cache entries in milliseconds
export const GLOBAL_CACHE_TTL = 60 * 1000; // 60 seconds
export const EXPIRED_CACHE_TTL = 5 * 1000; // 5 seconds buffer for cleanup
export const CACHE_CREATED_AT = "createdAt"; // field to optionally check creation time
